# NIF004 music — drop the 5 downloaded tracks here

From the YouTube Audio Library (see `../../08_conform/NIF004-AUDIO-GET-THESE.md`
for the picks + filter fallbacks). MP3 is fine. Name them exactly:

| file | track | act / beats |
|---|---|---|
| `1-act1.mp3` | Aakash Gandhi — "Lifting Dreams" | B00–B03 |
| `2-act2.mp3` | Aakash Gandhi — "Meridian" | B04–B11 |
| `3-act3.mp3` | Jeremy Blake — "Water Lily" | B12–B19 |
| `4-act4.mp3` | Asher Fulero — "Sleepless" | B20–B25 |
| `5-act5.mp3` | Asher Fulero — "Star Attractions" | B26–B33 |

Substitutes are fine — keep the number prefix so the Resolve step picks them up
in order. Drop any spares as `spare-*.mp3`.

These are **not** used by Remotion (music is the Resolve pass, not baked). This
folder is just the handoff point.
