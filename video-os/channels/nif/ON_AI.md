# — ON AI — (Nothing Is Free)

Two versions. Use the one that matches the asset policy **actually used in the episode**, and change this file and `identity.json` together if the policy changes (v2.0 §5.2, §Step 22).

## If the episode is 100% code (no generated stills anywhere)

```
— ON AI —
Narration uses an AI voice model trained on my own recorded voice. Every visual
in this video is built in code from the sources listed above — there are no
generated images or stock footage. AI assists research and scripting; every
editorial judgement is my own.
```

**YouTube (2026):** set **"Altered or synthetic content" = ON** at upload — a
cloned voice requires it. YouTube states the toggle does not reduce reach or
monetization. Separately, the "inauthentic content" rule (renamed from
"repetitious content", July 2025) demonetizes mass-produced / templated video —
the anti-template law (§ house style) keeps NIF on the safe side of it.

## If any generated still is used (background / mid plane only)

```
— ON AI —
Narration uses an AI voice model trained on my own recorded voice. Background
and environment imagery was AI-generated to my own briefs. Every diagram,
figure, chart, caption, character and piece of typography was built in code
from the sources listed above. Research, script, data, design and edit are my
own.
```

**NIF002 (the real-time ad auction):** 100% code — use the first version.
