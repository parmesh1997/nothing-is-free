# Recovery status — 2026-09-06 (post system reset)

A Windows reset wiped `C:\Users\parme\.claude` (session history, memory, plugins),
Python, FFmpeg, DaVinci Resolve, and git identity. **Everything on `D:\` survived
intact** — the whole `video-os` project, `node_modules`, whisper.cpp, all skills,
and the finished NIF003 render.

## Where NIF003 was (runbook §22.6)

| Step | State |
|---|---|
| 0–2 intake / script / reconcile | ✅ done (reconcile 2026-09-05, 20:41.0) |
| 3 build + render | ✅ done — 33 beats, `tsc` clean, `out/review/NIF003-FULL.mp4` rendered 14:42 (verified 20:41 / 1080p / audio baked) |
| 4 Resolve + audio finish | ⬜ **next** |
| 5 package + ship | ⬜ not started |

Last edits before the reset: `src/nif003/v4/beats.tsx` 13:27 → `Stage3D.tsx` 12:44
→ full render 14:42. Stopped at the Step 3 → 4 handoff.

## Environment — done vs. pending

**Done this session:**
- Python 3.12.10 (+ Pillow, numpy) → occupancy check runs
- FFmpeg 9.0.1
- git `safe.directory=*`
- Verified the Remotion engine works end-to-end (typecheck, `compositions`, `still`)
- Claude memory rebuilt (`~/.claude/projects/D--YT/memory/`)

**Done since:**
- **DaVinci Resolve Studio 21.0.4.5** — installed & running (same version as before).
- **davinci-resolve-mcp** — re-cloned to `C:\Users\parme\AppData\Local\davinci-resolve-mcp\` (v2.208.1), fresh py3.12 venv + deps, `scripts/doctor.py` all `[OK]` with a live scripting connection. `.mcp.json` fixed (`PYTHONHOME` 314→312); original saved as `.mcp.json.bak-2026-09-06`.

**You need to do:**
1. **Restart Claude Code / your terminal** so `python` and `ffmpeg` resolve by name (winget updated PATH after this process started), and the `davinci-resolve` MCP server loads / gets approved.
2. **Claude Code plugins** (TOOLKIT.md §0): `/plugin` install `last30days-skill`, `claude-video-vision`, `marketing-skills`, `superpowers`. GSAP skills are already on D:.
3. **Rotate** the `GEMINI_API_KEY` sitting in plaintext in `video-os/engine/remotion/.env`.
6. Set git identity: `git config --global user.name "..."` / `user.email "..."`.

## Known issues to fix (pre-existing, not from the reset)

- `scripts/finish-v4.mjs`, `mix-v4.mjs`, `hevc-master.mjs` are hard-coded to
  `NIF002` — parameterize by episode (or make NIF003 copies) before the Step 4
  master.
- `channels/nif/identity.json` drift: `motion.maxEventIntervalSeconds` is 3.5,
  should be 3.0 (CLAUDE.md / runbook §19); check `bloom.engine` (Skia removed).
- 3 citations still `[VERIFY]` in `episodes/NIF003/01_script/beats.md`
  (Nexstar station count · ESPN ≈ $7.57B vs FS1 ≈ $1.16B · sports = 60–90% of
  affiliate fees). VO is recorded, so a bad number = a re-record of that beat.
- `src/Root.tsx`: B29 (the reversal) has no Dark-Law tail applied yet.

## Occupancy note

A full-episode scan reads ~14.6% median ink coverage vs the old §2.2 floor of
22%. This is **not a new regression** — the creator deliberately moved past the
mechanical occupancy gate for NIF003 on 2026-09-06 (runbook §22.2 / §22.3): the
`shelf` + real-props staging is the accepted compromise, and the stricter
~70%-fill self-audit applies from NIF004 onward. A human eyes-on pass for
§22.5's two failure modes (frames that read empty, clipped/off-screen props) is
still worth doing before the finish.
